/*
 * File to pack msgs from IMU into JSON and unpack into GUI
 * 
 */


function response_callback(response) {
  document.getElementById("imu stuff");
	
} 

module.exports = response_callback

/*
var imuData = {
	// some IMU data.
}

output.innerHTML = imuData.x + imuData.y + imuData.z;

*/ 
